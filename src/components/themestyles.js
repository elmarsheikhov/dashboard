const themeStyles1 = {
  backgroundColor: darkTheme ? "#555" : "#fafafa ",
  color: darkTheme ? "#fafafa" : "#555",
};
// dashboardda qutular, sidebar.
const themeStyles2 = {
  backgroundColor: darkTheme ? "#444" : "#fafafa",
  color: darkTheme ? "#fafafa" : "#444",
};
export { themeStyles1, themeStyles2 };
